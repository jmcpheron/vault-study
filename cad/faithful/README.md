# Faithful variant

A CAD reinterpretation of Adam Savage's machined 1/12-scale vault door
as built on the Tested YouTube series. The geometry follows Adam's
dimensions exactly — same materials called out in [`../../specs.md`](../../specs.md),
same gear math, same combination-lock-as-gatekeeper.

## Onshape document

**Public, view-only:**
[cad.onshape.com/documents/eaf3e87c…](https://cad.onshape.com/documents/eaf3e87c1faae12ad867b335/w/83a96bb8921d1af3abd7aecd/e/9db299b535aaeded5de5120c)

The canonical IDs (document / workspace / element) and the pinned
Onshape **Version** for the current STEP live in
[`../../src/vaultkit/params.py`](../../src/vaultkit/params.py) under
`ONSHAPE["faithful"]` — the one place the tooling reads. Current STEP:
**live workspace export** (no named Version pinned yet).

The Onshape document is the **source of truth** for geometry. The
STEP export sits at [`../../step-source/unauthorized-vault-clone.step`](../../step-source/unauthorized-vault-clone.step)
(name kept as a wink in the inbox folder — the file represents the
faithful replica). When the Onshape document updates, re-export STEP
and replace that file — see [`../../SHAREABLE-CAD.md`](../../SHAREABLE-CAD.md)
for the cut-a-Version → export → commit loop.

## Modeling conventions

See [`onshape.md`](onshape.md) for FeatureScript settings and the
Assembly-tab mate strategy that turns one modelled spur into the
timed 12-pin mechanism.

## Bill of materials

_(Will be auto-generated by `vaultkit bom` once the heavy pipeline
lands. For now, see [`../../specs.md`](../../specs.md) — every part
and dimension is enumerated there.)_

## Differences from Adam's build (acknowledged, not "fixes")

- We aren't actually building a physical one in cast iron and brass.
  CAD takes the geometry; the materials are FYI.
- The lapping-plate flatness work, the hand-threading concentricity
  fights, the dividing-plate fixturing — all the rich physical-shop
  knowledge from the videos — exists in the docs as commentary
  ([`docs/build-log.md`](../../docs/build-log.md),
  [`docs/sources.md`](../../docs/sources.md)) but doesn't change the
  CAD.

## Related

- [`../fdm/`](../fdm/) — the printable-at-home variant. Same
  mechanism, redesigned for FDM.
- [`../../docs/mechanics.md`](../../docs/mechanics.md) — how the
  whole thing works as a single system.
